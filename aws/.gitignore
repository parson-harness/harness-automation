# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
*.tfplan

# Crash log files
crash.log

# Note: tfvars files are tracked in this repo (no secrets stored here)
# If you add secrets, use environment variables or a secrets manager instead

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
planoutput.txt
plan.txt

*.DS_Store*

# Local outputs (generated, contains environment-specific values)
outputs.local.json